{
  "name": "webmenu-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
  "build": "npm install --prefix client && npm run build --prefix client"
},
  "devDependencies": {
    "prisma": "6.4.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@prisma/client": "^7.4.2",
    "@google/generative-ai": "^0.24.1"
  }
}